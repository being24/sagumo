services:
  sagumo:
    image: "ghcr.io/being24/sagumo:latest"
    volumes:
      - ./data:/workspaces/sagumo/data
      - ./log:/workspaces/sagumo/log
    env_file:
      - ./.env
    restart: always
